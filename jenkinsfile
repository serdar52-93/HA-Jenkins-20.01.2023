pipeline {
  agent any
  tools {
    maven 'maven-3.8.7'
  }
  stages {
    stage ('Build') {
      steps {
        sh 'mvn install'
      }
    }

    stage ('Test') {
        steps {
         echo "Test" 
         sh 'mvn -version'
        }
    }

    stage('Deploy') { 
        steps {   
         echo "Deploy" 
        }
    } 
  }
}